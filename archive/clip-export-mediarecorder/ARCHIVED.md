# Archived clip exporter

This directory preserves the original canvas and `MediaRecorder` implementation.
It is not loaded by the application. The live exporter uses Mediabunny and lives
in `services/clipExportService.js` and `utils/plyr-plugin-clip-export.js`.
